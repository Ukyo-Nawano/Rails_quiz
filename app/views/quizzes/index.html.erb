<h1 class="font-bold text-2xl text-center pt-24 text-green-100">みんなのクイズ一覧</h1>
<div class="flex flex-wrap">
  <% @quizzes.each do |quiz| %>
    <div class="w-1/3">
      <%= link_to quiz_questions_path(quiz) do %>
        <%= image_tag quiz.image, alt: "milmil", class: "w-1/5" %>
        <p><%= quiz.title %></p>
      <p>作成者: <%= quiz.user.name %></p>
      <p>タグ:
        <% quiz.tags.each do |tag| %>
          <span class="tag"><%= tag.tag %></span>
        <% end %>
      </p>
      <%= link_to "質問を表示", quiz_questions_path(quiz), class: "text-blue-500" %>
      <% end %>
    </div>
  <% end %>
</div>



<%= button_to 'Logout', 'auth/logout', method: :get, data: { turbo: false } %>
<%# <%= debug(@user) %>  <!-- @userの内容を表示 -->
<%# <%= JSON.pretty_generate(@user) %>

<div>
  <p>User Name: <%= @user["name"] %></p>
  <p>User Email: <%= @user["email"] %></p>
  <p>User Nickname: <%= @user["nickname"] %></p>
  <p>User Image: <%= @user["image"] %></p>
</div>