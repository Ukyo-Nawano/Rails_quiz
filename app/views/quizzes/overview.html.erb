<div class="quiz-overview text-center mt-8 bg-white shadow-md rounded-lg p-8 max-w-4xl mx-auto pt-24">
<h1 class="text-4xl font-bold text-[#3DB8A4] mb-4"><%= @quiz.title %></h1>
<p class="text-lg text-gray-700 mb-6"><%= @quiz.description %></p>

<% if @quiz.image.attached? %>
    <%= image_tag @quiz.image, alt: "Quiz Image", class: "w-full max-w-md mx-auto mb-6 rounded-lg shadow" %>
<% end %>

<p class="text-xl font-semibold text-gray-800">このクイズには <%= @quiz.questions.count %> 問の設問があります。</p>

<div class="mt-8 flex justify-center gap-4">
    <% if @is_owner %>
    <%= link_to "編集する", edit_quiz_path(@quiz), class: "px-4 py-2 bg-green-100 text-white rounded-lg shadow-md hover:bg-green-75" %>
    <%= link_to "削除する", confirm_destroy_quiz_path(@quiz), method: :get, class: "px-4 py-2 bg-red-500 text-white rounded-lg shadow-md hover:bg-red-600", data: { confirm: "本当に削除しますか？" } %>
    <% else %>
    <% if @first_question %>
        <%= link_to "開始する", quiz_question_path(@quiz, @first_question), class: "px-6 py-2 bg-green-100 text-white rounded-lg shadow-md hover:bg-green-75" %>
    <% else %>
        <p class="text-red-500 font-semibold">このクイズには設問がありません。</p>
    <% end %>
    <% end %>
    <%= link_to "戻る", quizzes_path, class: "px-4 py-2 bg-gray-300 text-gray-700 rounded-lg shadow-md hover:bg-gray-400" %>
</div>
</div>